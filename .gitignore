# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# SCADA-LTS / Docker local state
scada/data/
scada/mysql/
*.sqlite
*.db

# Secrets / local config
.env
*.local
secrets.*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build / logs / captures
*.log
*.pcap
*.pcapng
dist/
build/

# Large local-only artifacts (kept on disk, not in the repo)
# 16 MiB Opta QSPI backup (WiFi fw + FAT fs); checksum in opta/backup/README.md.
opta/backup/opta_qspi_flash.bin
# Vendor reference copied from the plccable USB (PDFs, samples, bundled installer).
plccable-reference/
